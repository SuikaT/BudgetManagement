import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "com.suika.app",
    appName: "budget-management-frontend",
    webDir: "dist/budget-management-frontend/browser",
};

export default config;
